#include "holberton.h"

/**
 * _isupper - is the function that returns uppercase
 * @c: is the character to checked
 * Return: 1 if c is uppercase, otherwise 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
