#include "holberton.h"
/**
 * _isdigit - is a function that checks for a digit
 * @c: is the checker
 * Returns: 1 if c is a digit, otherwise 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
